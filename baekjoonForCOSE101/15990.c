#include <stdio.h>
#include <stdlib.h>

int division(int a, int b) {
    return a % b;
}

if(temp % 3 == 0) return 1;
else if()

int main() {
    int n;
    scanf("%d", &n);

    int *list = malloc(sizeof(int) * n);
    for(int i = 0; i < n; i++) {
        scanf("%d", &list[i]);
    }

    for(int i = 0; i < n; i++) {
        int temp = list[i];
        temp / 3
    }

    free(list);
    return 0;
}